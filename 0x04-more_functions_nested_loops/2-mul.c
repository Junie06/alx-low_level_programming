#include "main.h"

/**
 * mul - Function that multiplies two integers
 * @a: first argument
 * @b: second argument passed
 * @c: result
 * Return: c (Success)
 */
int mul(int a, int b)
{
	int c;

	c = a*b;

	return (c);
}
